package controlador;

import modelo.*;
import modelo.casas.*;
import modelo.cartas.*;
import visao.*;
import java.util.*;
import java.io.*;

public class SalvarJogo{

    public static void salvarJogo() {
        //Jogo jogo = Jogo.getInstancia();
        
        /*
        Jogo salvarJogo = Jogo.getInstancia();
        Gson gson = new Gson();

        try(FileWriter writer = new FileWriter("jogoSalvo.json")){
            gson.toJson(jogo, writer);
            System.out.println()"jogo salvo com Json";
        } catch (IOException e){
            e.printStackTrace();
        }*/
    }
}

